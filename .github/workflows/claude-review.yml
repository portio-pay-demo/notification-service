name: Claude Code Review
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    uses: port-io-pay/platform-workflows/.github/workflows/claude-code-review-base.yml@main
    secrets: inherit  # passes ANTHROPIC_API_KEY through automatically
