name: Claude Code Review
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    uses: portio-pay-demo/platform-workflows/.github/workflows/claude-code-review-base.yml@main
    secrets: inherit  # passes ANTHROPIC_API_KEY through automatically
